# app:
#     resource: '@AppBundle/Controller/'
#     type: annotation

login:
  path: /
  defaults: { _controller: AppBundle:Auth:login }

home:
  path: /home
  defaults: { _controller: AppBundle:Home:index }

character_list:
  path: /characters
  defaults: { _controller: AppBundle:Character:index }

character_show:
  path: /characters/{id}
  defaults: { _controller: AppBundle:Character:show }

favourite_add:
  path: /favourites/add
  defaults: { _controller: AppBundle:Favourite:add }